Task: WAP in c to find the sum of n natural numbers:

Code:
#include <stdio.h>
#include <conio.h>

void main(){
  int i, n, sum=0;
  printf("enter n:");
  scanf("%d",&n);
  for (i=1, i<=n; i++){
    sum=sum+i;
  }
  printf("Sum:%d",sum);
  getch();
}

